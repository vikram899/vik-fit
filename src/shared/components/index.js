/**
 * SHARED COMPONENTS BARREL EXPORT
 *
 * Centralized export of all shared components (UI and feature components).
 *
 * Imports:
 * - Base UI: import { Card, Button, Section } from '@shared/components'
 * - Or specific: import { Card, Button, Section } from '@shared/components/ui'
 */

// Base UI Components
export { Card, Button, LogButton, Section, Badge, ProgressBar, StatCard, MacroCard, ListItem } from './ui';

// Future: Feature-specific shared components can be added here
// export { MealCard, WorkoutCard } from './meals';
// export { ExerciseCard } from './workouts';
